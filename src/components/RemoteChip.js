import React from "react"

export default function RemoteChip() {
  return (
    <div
      style={{
        backgroundColor: "#ffdb99",
        borderRadius: "40px",
        padding: "2px 10px",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      <span style={{ fontSize: "12px", color: "#b37300" }}>remote</span>
    </div>
  )
}
